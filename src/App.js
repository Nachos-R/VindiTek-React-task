import React, { StrictMode } from 'react';
import AppContainer from './containers/AppContainer';

const App = () => (
  <div className="App">
    <StrictMode>
      <AppContainer />
    </StrictMode>
  </div>
);

export default App;
